/** Realtime Broadcast topic for catalog cache invalidation (public). */
export const CATALOG_VERSION_TOPIC = "catalog-version";

/** Event name carried in Broadcast payload `{ versionAt: string }`. */
export const CATALOG_VERSION_EVENT = "catalog_version_changed";
